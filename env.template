# Database
DATABASE_URL=mysql://fieldvisits:fieldvisits_dev_password@127.0.0.1:3306/field_visits

# Session signing secret — replace with a long random value before production
JWT_SECRET=replace-with-a-long-random-secret

# Runtime
NODE_ENV=development
PORT=3000
UPLOAD_DIR=uploads

# Demo is convenient for local evaluation only; set false before real use
DEMO_MODE_ENABLED=true

# Initial Administrator (Created on first run if no admin exists; default: admin / Admin@123456)
# INITIAL_ADMIN_OPENID=admin
# INITIAL_ADMIN_PASSWORD=Admin@123456
# INITIAL_ADMIN_EMAIL=admin@sindean.local
